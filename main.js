const submitBtn = document.getElementsByClassName("submitBtn")[0];

function handleErrorState(){

}
function handleRequired(){
  /* 

    Check if firstName, lastName, email, queryType, message and isConsentGiven filled out

  */
}

submitBtn.addEventListener("submit", (e) => {
  /*

    Handle error-handling top to bottom
      a. Check if required field is missing
      b. Check if the email address is not formatted correctly

    1. handleRequired() to check if the inputs are empty or not
  */
});